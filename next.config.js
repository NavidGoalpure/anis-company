/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    formats: ['image/webp'],
    disableStaticImages: false,
  },
};

module.exports = nextConfig;
